Based on hello world

- Start server?
> sharpie-web runserver

- Best practices?
> sharpie-install fetch (every time)
OR
> sharpie-install fetch --check (every time)
> sharpie-install fetch (at the end)

Actually most of the docs are obsolete with sharpie-install

Redo the intro:
- in quickstart remind people of needing to have the db initialized (or unhelpful error)
- In hello world, we still need to know about the db but also about the secret key (only talked about in quickstart)

> Make the connection key / default consent with sharpie-install?

Quickstart talks about the key. Hello world does not :(

No default consent

Filenames in config yaml are local to experiment folder. No warnings beforehand though
